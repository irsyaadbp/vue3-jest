function calculate(a: number, b: number) {
  return a + b;
}
// test("should 1 + 1 = 2", () => {
//   expect(calculate(1, 1)).toBe(2);
// });

// it("should 4 + 4 = 8", () => {
//   expect(calculate(4, 4)).toBe(8);
// });

describe("BaseButton positif case", () => {
  it("should display label", () => {
    // expect(calculate(4, 4)).toBe(8);
  });

  it("should display label 2", () => {
    // expect(calculate(4, 4)).toBe(8);
  });
  it("should display label 3", () => {
    // expect(calculate(4, 4)).toBe(8);
  });
});
